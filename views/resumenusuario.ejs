<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resumen del Usuario - SISTEMA BANCARIO</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-image: url("./imagen/ahorro.jpg");
        margin: 0;
        padding: 20px;
        display: flex;
        flex-direction: column;
        align-items: center;
        color: #333;
      }
      h1 {
        color: #5171b4;
        margin-bottom: 20px;
        text-shadow: 2px 2px 2px rgba(52, 77, 100, 0.548);
        font-weight: bold;
      }
      .user-details {
        background-color: #e9afe9;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
        text-align: left;
      }
      .user-details p {
        margin: 10px 0;
      }
      .user-details p span {
        font-weight: bold;
        color: #5d5d5d;
      }
      .back-link {
        padding: 10px 20px;
        background-color: #995bc2;
        color: #fff;
        border: none;
        border-radius: 28px;
        cursor: pointer;
        transition: background-color 0.3s;
        margin-top: 20px;
      }
      .back-link:hover {
        color: #1e062e;
      }
    </style>
  </head>
  <body>
    <h1>Resumen del Usuario</h1>
    <div class="user-details">
    <h2>Usuario</h2>
      <p><span>ID:</span> <%= usuario.usuarioId %></p>
      <p><span>Nombre:</span> <%= usuario.nombre %></p>
      <h2>Cuenta del Usuario</h2>
      <p><span>Balance:</span> <%= usuario.ahorroBalance || usuario.prestamoBalance %></p>
      <p><span>Tasa de Interes:</span> <%= usuario.ahorroTasaInteres || usuario.prestamoTasaInteres %></p>
      <% if (usuario.cooperativaId) { %>
        <h2>Cooperativa</h2>
        <p><span>Próximo aporte:</span> <%= usuario.fechaProximoAporte %></p>
        <p><span>Fecha de pago:</span> <%= usuario.fechaPago %></p>
        <p><span>Recurrencia de pago:</span> <%= usuario.recurrencia %></p>
        <p><span>Fecha de fin:</span> <%= usuario.fechaFin || "Por definir" %></p>
        <p><span>Total aportado:</span> <%= usuario.montoAportado %></p>
      <% } %>
      <!-- <h2>Cuenta del Usuario</h2>
      <p><span>Saldo:</span> <%= usuario.saldo %></p>
      <p><span>Tasa de Interes:</span> <%= usuario.tasaInteresCuenta %></p>
      <p><span>Tipo de cuenta:</span> <%= usuario.tipoCuenta %></p>
      <p><span>Cooperativa:</span> <%= usuario.nombreCoop %></p> -->
    </div>
    
    <a class="back-link" href="/usuarios">Volver a la lista de usuarios</a>
    <a class="back-link" href="http://localhost:3001/cooperativas/abonar/<%= usuario.usuarioId %>">Abonar cooperativa</a>
  </body>
</html>
